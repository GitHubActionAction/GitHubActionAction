name: Workflow A

on:
  push:

jobs:
  trigger-workflow-b:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Workflow B on Different Branch
        uses: actions/github-script@v5
        with:
          github-token: ${{ secrets.PUPLIC_TOKEN }}
          script: |
            const { data } = await github.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'trigger_workflow.yml',  // Replace with actual workflow file name
              ref: 'main',  // Replace with the target branch name
              inputs: {
                // Define any input parameters here
              }
            });
            console.log(data);




